# EV Charge Book v0.5 Implementation Status

Last synced: 2026-08-27
Status: Core state flows implemented / physical acceptance pending

## Baseline

- PR #71: v0.5 Dark First UI redesign merged.
- Hero vehicle artwork direction: generate the complete visual asset externally (car, aurora/environment light, reflection) and let Android display the finished asset. Do not spend Compose complexity reproducing those effects.
- UI work continues to follow the approved screenshots/design language while keeping data truth above decorative completeness.

## Documentation

PR #76 is documentation-only and defines the VehicleState / Charge / Trip state direction:

- `VEHICLE_STATE_ARCHITECTURE_v0.5.md`
- `CHARGE_TRIP_UX_FLOW_v0.5.md`
- `V0.5_IMPLEMENTATION_ROADMAP.md`
- this status file

Trip reliability authority remains:

- `LOCATION_TRIP.md`
- `TRIP_GPS_RELIABILITY_AND_SPEED_VISUALIZATION.md`

## Vehicle State Foundation — PR #79

Status: merged to `main`; Android Build green before merge.

Implemented:

- `VehicleStateEntity` as the shared current SOC/mileage state per vehicle
- `VehicleStateDao` / repository / manager
- Room schema foundation and migration backfill
- unknown historical values remain null instead of being fabricated
- current-state reconstruction respects event ordering rather than blindly trusting the last edited record

## Smart Charging Flow — PR #81

Status: merged to `main`; Android Build green before merge.

Implemented:

- start SOC inherits current VehicleState, then falls back to the latest valid charge state
- current mileage inherits VehicleState
- end SOC defaults from recent charging context where appropriate
- five icon-distinguished charging types: 家充 / 公共慢充 / 公共快充 / 超充 / 闪充
- same-type recent unit price memory and recent charging presets
- billable charger/meter energy separated in the UX from vehicle-received energy estimate
- charging loss estimate and anomaly warning
- total cost auto-calculation with manual override
- new charge prefers current location while retaining manual/common-place choices
- charge create/edit screens prioritize actual input instead of a large decorative Hero block
- charge completion updates current VehicleState SOC/mileage atomically
- backdated charge create/edit/delete rebuilds current state instead of rewinding it blindly

## Trip Reliability / Detail — PRs #80 / #82 / #83 / #85

Status: merged to `main`; CI green for each implementation slice. Physical acceptance remains open.

Implemented:

- Location callback liveness separated from app-level stationary heartbeat
- trusted GPS-quality gating for max-speed peaks
- altitude facts and resolved endpoint addresses
- distinct start/end semantic route markers
- trusted speed-based route coloring while unknown/untrusted segments remain neutral gray
- visible GPS gaps rather than fake continuous routes

Physical owners remain #77 / #78 / #67 / #66 / #69 / #42.

## Trip SOC / Energy Flow — PR #87

Status: merged to `main` as `9a0daed2e93e9e7dd56a89f51f51444ff6500295`; Android Build #336 green.

Implemented:

- trip start snapshots current VehicleState SOC/mileage
- Room schema adds trip start/end SOC, mileage, consumed-energy estimate and average-consumption estimate fields
- existing historical trips keep unknown fields null rather than receiving fabricated values
- trip end requires explicit end SOC
- end mileage is prefilled from start mileage + recorded trusted GPS distance when possible and remains editable
- consumed energy is derived from battery capacity and a meaningful positive SOC drop
- average consumption is derived as estimated kWh/100km from estimated energy and recorded GPS distance
- unchanged/increased SOC or insufficient inputs produce unavailable consumption, not a fake zero/positive number
- trip completion atomically persists Trip end state and updates VehicleState SOC/mileage
- backup schema preserves the new Trip fields and remains backward-readable
- Trip READY shows current SOC/current mileage, start CTA and recent trips without drawing a fake pre-start location/route
- ongoing notification opens the Trip flow instead of directly completing it, because end SOC confirmation is required
- stale legacy direct-stop intents cannot silently bypass the completion form

PR #84 is closed without merge and superseded by #87.

## Trip Energy Presentation — PR #88

Status: merged to `main` as `2841b78c508af5657b5013cc0c06b3c47bba5f0f`; Android Build #339 green.

Implemented:

- LIVE panel shows the trip-start SOC snapshot without pretending it is live vehicle telemetry
- LIVE explicitly states that current SOC/realtime energy are not inferred while driving
- history rows show SOC range and estimated consumption when available
- Trip detail adds a dedicated `能耗与车辆状态` section for SOC, estimated consumed kWh, estimated kWh/100km and odometer range
- unchanged/increased SOC remains unavailable rather than becoming fake zero or negative consumption
- existing GPS gap handling, altitude/address facts, endpoint semantics and trusted speed-colored route rendering are preserved

## Home / Vehicle State Presentation — PR #89

Status: active implementation PR.

Scope:

- Dashboard Hero shows current SOC, current mileage and battery capacity from VehicleState
- Vehicle page Hero shows the same selected vehicle state
- unknown values remain `--`
- generated Hero artwork remains asset-driven; no runtime visual-effect expansion is introduced

## Current State Consistency Rule

VehicleState is the current-state projection, not a replacement for historical facts.

- Charge / completed Trip events update current state.
- Backdated edits/deletes trigger state reconstruction from remaining valid events.
- Unknown historical values remain unknown.
- A later valid/manual current state must not be overwritten by an older event merely because that event was edited later.

## Data-Quality Rule

SOC-derived energy is an estimate, not BMS telemetry.

Short trips and integer SOC rounding can make the estimate unreliable. Regenerative/downhill driving or mid-trip charging can also cause unchanged/increased SOC. In those cases the UI must show unavailable/estimated state rather than force a precise consumption value.

GPS-derived mileage prefill is also an aid, not an odometer fact. Users can correct end mileage before completion.

## Remaining v0.5 Work

Current priority:

1. Finish PR #89 and verify Home / Vehicle current-state presentation.
2. Physical Trip acceptance for background other-app continuity, stationary red lights, max-speed parity, address/altitude and trusted route colors.
3. Verify the complete Trip READY -> LIVE -> end SOC -> VehicleState update flow on a real device.
4. Review Energy/Statistics presentation using Charge facts and Trip estimates without mixing their semantics.
5. Replace Hero artwork with generated per-model finished assets and finish five-page physical visual acceptance.
6. Continue accessibility / large-font / small-screen / TalkBack closeout.

Cloud sync expansion remains deferred until local v0.5 experience is stable.

## Notification Follow-up

Because Trip completion now needs explicit end SOC, the foreground notification no longer owns a direct `COMPLETED` write.

Future #26 work should deep-link `打开行程` directly to the active Trip completion UI. It must not restore a shortcut that skips end-SOC confirmation.

## Non-goals for this pass

- speculative AI features
- social/community features
- unnecessary cloud complexity
- Compose-only recreation of photorealistic Hero lighting/reflection
- fabricating GPS, SOC, mileage or energy merely to make a screen look complete
