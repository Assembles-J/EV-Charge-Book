(function(){
  const $=id=>document.getElementById(id);
  const KEY_RE=/^[a-z0-9][a-z0-9-]{1,100}$/;
  const IMAGE_RE=/\.(png|webp)$/i;
  const MIN_CONTRAST=3;
  let catalog=null,manifest=null,bundle=null,bundleReview=null,files=[],rows=[];

  function normalizeToken(value){return String(value||'').trim().toLowerCase().replace(/\.(png|webp)$/i,'').replace(/[\s_]+/g,'-').replace(/[^a-z0-9-]+/g,'-').replace(/-+/g,'-').replace(/^-|-$/g,'')}
  function safeSlug(value){return String(value||'').toLowerCase().replace(/[^a-z0-9]+/g,'_').replace(/^_+|_+$/g,'')}
  function stripHeroVariant(value){return String(value||'').trim().toLowerCase().replace(/-(dark|light)$/,'')}
  function heroVariantKey(base,variant){return `${stripHeroVariant(base)}-${variant}`}
  function srgb(value){const c=Math.max(0,Math.min(255,Number(value)||0))/255;return c<=.04045?c/12.92:Math.pow((c+.055)/1.055,2.4)}
  function rgbLuminance(r,g,b){return .2126*srgb(r)+.7152*srgb(g)+.0722*srgb(b)}
  function contrastRatio(a,b){const hi=Math.max(a,b),lo=Math.min(a,b);return (hi+.05)/(lo+.05)}
  const LIGHT_L=rgbLuminance(245,246,245),DARK_L=rgbLuminance(8,17,13);
  function longestPrefix(fileName,targets){const stem=normalizeToken(fileName);let best=null;for(const target of targets||[]){for(const alias of target.aliases||[]){if(alias&&stem.startsWith(alias)&&(!best||alias.length>best.alias.length))best={target,alias,stem}}}return best}
  function fieldDiff(current,next,fields){return fields.flatMap(field=>{const before=current?.[field]??null,after=next?.[field]??null;return JSON.stringify(before)===JSON.stringify(after)?[]:[{field,before,after}]})}
  function inferVariant(fileName){const tokens=normalizeToken(fileName).split('-');if(tokens.includes('light'))return 'light';if(tokens.includes('dark'))return 'dark';return ''}
  function heroExistingInfo(base,stateManifest=manifest){const clean=stripHeroVariant(base),artworks=stateManifest?.artworks||{};return ['dark','light'].map(variant=>{const key=heroVariantKey(clean,variant),exact=artworks[key],legacy=variant==='dark'?artworks[clean]:null;return {variant,key,current:exact||legacy||null,legacy:!exact&&!!legacy}})}
  function nextHeroFilename(base,variant,stateManifest=manifest){const info=heroExistingInfo(base,stateManifest).find(x=>x.variant===variant);return `${safeSlug(heroVariantKey(base,variant))}_v${Number(info?.current?.version||0)+1}.webp`}
  function nextLogoFilename(brand,variant){const v=Number(variant==='light'?brand?.logoLightVersion:brand?.logoDarkVersion)||0;return `brand_${safeSlug(brand?.brandId)}_${variant}_v${v+1}.webp`}
  globalThis.__evResourceWorkbenchTest={normalizeToken,safeSlug,stripHeroVariant,heroVariantKey,longestPrefix,fieldDiff,inferVariant,nextHeroFilename};
  if(typeof document==='undefined')return;

  function brandTargets(){const all=[...(catalog?.brands||[])],staged=bundle?.catalogImport?.brands?.[0];if(staged&&!all.some(x=>x.brandId===staged.brandId))all.push({...staged,logoKey:`brand-${staged.brandId}`,logoLightVersion:0,logoDarkVersion:0});return all.map(brand=>({id:brand.brandId,brand,aliases:[normalizeToken(brand.brandId),normalizeToken(brand.logoKey),normalizeToken(`brand-${brand.brandId}`)].filter(Boolean)}))}
  function heroTargets(){const keys=new Set();for(const vehicle of catalog?.vehicles||[]){if(vehicle.heroArtworkKey)keys.add(stripHeroVariant(vehicle.heroArtworkKey))}for(const key of Object.keys(manifest?.artworks||{}))keys.add(stripHeroVariant(key));if(bundle?.heroKey)keys.add(stripHeroVariant(bundle.heroKey));return [...keys].filter(Boolean).map(key=>({id:key,key,aliases:[normalizeToken(key),normalizeToken(`hero-${key}`)]}))}
  function brandById(id){return brandTargets().find(x=>x.id===id)?.brand||null}
  async function fetchJson(url){const r=await fetch(url,{headers:{Accept:'application/json'}}),data=await r.json();if(!r.ok)throw new Error(data.error||`${url} 读取失败`);return data}
  async function loadState({rebuild=true}={}){try{[catalog,manifest]=await Promise.all([fetchJson('../api/catalog/state'),fetchJson('../api/state')]);$('runtimeSummary').textContent=`Catalog v${catalog.catalogVersion||'?'} · 品牌 ${(catalog.brands||[]).length} · 车型 ${(catalog.vehicles||[]).length} · Hero 资源 ${Object.keys(manifest.artworks||{}).length}`;if(bundle)revalidateCurrentBundle();if(rebuild&&files.length)await rebuildRows();else updatePublishGate()}catch(error){$('runtimeSummary').textContent=`读取失败：${error.message||error}`}}

  function normalizeBundle(raw){if(!raw||typeof raw!=='object')throw new Error('JSON 必须是对象');if(raw.format==='ev-charge-book-resource-bundle'){if(Number(raw.version)!==1)throw new Error('resource bundle version 仅支持 1');if(raw.catalogImport?.format!=='ev-charge-book-vehicle-catalog'||Number(raw.catalogImport?.version)!==1)throw new Error('catalogImport 必须是 ev-charge-book-vehicle-catalog v1');return raw}if(raw.format==='ev-charge-book-vehicle-catalog'&&Number(raw.version)===1){return {format:'ev-charge-book-resource-bundle',version:1,heroKey:raw.vehicles?.[0]?.heroArtworkKey||'',catalogImport:raw,assets:{brandLogo:{light:'',dark:''},hero:{dark:'',light:''}}}}throw new Error('仅支持 resource bundle v1 或单条 vehicle catalog v1 JSON')}
  function inspectBundle(next){const brands=next.catalogImport?.brands||[],vehicles=next.catalogImport?.vehicles||[];if(brands.length!==1||vehicles.length!==1)throw new Error('工作台一次只接受 1 个品牌 + 1 个车型');const brand=brands[0],vehicle=vehicles[0];if(!KEY_RE.test(String(brand.brandId||'')))throw new Error('brandId 无效');if(!KEY_RE.test(String(vehicle.catalogId||'')))throw new Error('catalogId 无效');if(vehicle.brandId!==brand.brandId)throw new Error('vehicle.brandId 必须与品牌一致');const heroKey=stripHeroVariant(next.heroKey||vehicle.heroArtworkKey||'');if(!KEY_RE.test(heroKey))throw new Error('heroKey 无效');next.heroKey=heroKey;vehicle.heroArtworkKey=heroKey;return {brand,vehicle,heroKey}}
  function buildBundleReview(info){const currentBrand=(catalog?.brands||[]).find(x=>x.brandId===info.brand.brandId),currentVehicle=(catalog?.vehicles||[]).find(x=>x.catalogId===info.vehicle.catalogId);const brandFields=['name','englishName','isActive'],vehicleFields=['brandId','series','modelName','modelYear','trimName','powertrainType','batteryCapacityKwh','rangeKm','rangeStandard','heroArtworkKey','isActive'];const brandDiff=currentBrand?fieldDiff(currentBrand,info.brand,brandFields):[],vehicleDiff=currentVehicle?fieldDiff(currentVehicle,info.vehicle,vehicleFields):[];const heroRefs=(catalog?.vehicles||[]).filter(x=>x.heroArtworkKey===info.heroKey&&x.catalogId!==info.vehicle.catalogId),existingHero=heroExistingInfo(info.heroKey).filter(x=>x.current);return {info,currentBrand,currentVehicle,brandDiff,vehicleDiff,heroRefs,existingHero,requiresConfig:brandDiff.length>0||vehicleDiff.length>0,requiresHero:existingHero.length>0||heroRefs.length>0}}
  function renderBundleReview(review){const root=$('bundleDiff');root.innerHTML='';const add=(scope,d)=>{const row=document.createElement('div');row.className='diff-row warn';row.innerHTML=`<div class="diff-key">${escapeHtml(scope)} · ${escapeHtml(d.field)}</div><div class="diff-before">当前：${escapeHtml(show(d.before))}</div><div class="diff-after">将变为：${escapeHtml(show(d.after))}</div>`;root.appendChild(row)};review.brandDiff.forEach(d=>add(`品牌 ${review.info.brand.brandId}`,d));review.vehicleDiff.forEach(d=>add(`车型 ${review.info.vehicle.catalogId}`,d));for(const h of review.existingHero){const row=document.createElement('div');row.className='diff-row warn';row.innerHTML=`<div class="diff-key">Hero ${escapeHtml(h.key)}</div><div class="diff-before">当前 v${h.current.version}${h.legacy?' · legacy':''}</div><div class="diff-after">对应图片发布时会生成下一不可变版本</div>`;root.appendChild(row)}if(review.heroRefs.length){const row=document.createElement('div');row.className='diff-row warn';row.innerHTML=`<div class="diff-key">Hero Key 被其他车型复用</div><div class="diff-before">${review.heroRefs.map(x=>escapeHtml(x.catalogId)).join('<br>')}</div><div class="diff-after">更新会影响所有引用该语义 Key 的车型。</div>`;root.appendChild(row)}$('configUpdateConfirmWrap').classList.toggle('hidden',!review.requiresConfig);$('heroUpdateConfirmWrap').classList.toggle('hidden',!review.requiresHero);if(!review.requiresConfig)$('configUpdateConfirm').checked=false;if(!review.requiresHero)$('heroUpdateConfirm').checked=false}
  function validateBundle(){const text=$('bundleJson').value.trim();if(!text){bundle=null;bundleReview=null;$('bundleSummary').textContent='未载入资源包。只批量上传图片时可以跳过本步骤。';$('bundleDiff').innerHTML='';updatePublishGate();return null}try{bundle=normalizeBundle(JSON.parse(text));const info=inspectBundle(bundle);bundleReview=buildBundleReview(info);renderBundleReview(bundleReview);$('bundleSummary').textContent=`校验通过 · ${bundleReview.currentBrand?'已有':'新'}品牌 ${info.brand.brandId} · ${bundleReview.currentVehicle?'已有':'新'}车型 ${info.vehicle.catalogId} · Hero Key ${info.heroKey}${bundleReview.requiresConfig?' · 配置有变动':''}${bundleReview.requiresHero?' · Hero 已存在/复用':''}`;if(files.length)void rebuildRows();else updatePublishGate();return bundleReview}catch(error){bundle=null;bundleReview=null;$('bundleSummary').textContent=`资源包错误：${error.message||error}`;$('bundleDiff').innerHTML='';updatePublishGate();return null}}
  function revalidateCurrentBundle(){if(!bundle)return;try{const info=inspectBundle(bundle);bundleReview=buildBundleReview(info);renderBundleReview(bundleReview)}catch(error){bundleReview=null;$('bundleSummary').textContent=`资源包状态校验失败：${error.message||error}`}}

  async function analyzeImage(file){if(typeof createImageBitmap!=='function')throw new Error('浏览器不支持图片像素校验');const bitmap=await createImageBitmap(file);try{const size=96,canvas=document.createElement('canvas');canvas.width=size;canvas.height=size;const ctx=canvas.getContext('2d',{willReadFrequently:true});ctx.clearRect(0,0,size,size);ctx.drawImage(bitmap,0,0,size,size);const p=ctx.getImageData(0,0,size,size).data;let visible=0,transparent=0,lum=0,weight=0;for(let i=0;i<p.length;i+=4){const a=p[i+3];if(a<245)transparent++;if(a<24)continue;const w=a/255;visible++;lum+=rgbLuminance(p[i],p[i+1],p[i+2])*w;weight+=w}return {width:bitmap.width,height:bitmap.height,ratio:bitmap.width/bitmap.height,visiblePixels:visible,transparentRatio:transparent/(size*size),averageLuminance:weight?lum/weight:0}}finally{bitmap.close?.()}}
  function inferKind(name,stats){const t=normalizeToken(name).split('-');if(t.includes('logo')||t.includes('brand'))return 'logo';if(t.includes('hero'))return 'hero';return stats.ratio>=1.25?'hero':'logo'}
  function autoLogoVariant(stats){const l=contrastRatio(stats.averageLuminance,LIGHT_L),d=contrastRatio(stats.averageLuminance,DARK_L);if(l>=MIN_CONTRAST&&d>=MIN_CONTRAST)return 'both';if(l>=MIN_CONTRAST)return 'light';if(d>=MIN_CONTRAST)return 'dark';return 'light'}
  function validateLogo(stats,variant){if(Math.max(stats.width,stats.height)<512)return 'Logo 最长边至少 512 px';if(stats.visiblePixels<32)return '未检测到有效 Logo 主体';if(stats.transparentRatio<.02)return 'Logo 背景基本不透明';const l=contrastRatio(stats.averageLuminance,LIGHT_L),d=contrastRatio(stats.averageLuminance,DARK_L);if((variant==='light'||variant==='both')&&l<MIN_CONTRAST)return `浅色界面对比度不足 ${l.toFixed(1)}`;if((variant==='dark'||variant==='both')&&d<MIN_CONTRAST)return `深色界面对比度不足 ${d.toFixed(1)}`;return ''}
  function validateHero(stats){if(stats.width<1200||stats.height<800)return `Hero 至少 1200×800，当前 ${stats.width}×${stats.height}`;if(stats.ratio<1.40||stats.ratio>1.55)return `Hero 比例需 1.40–1.55:1，当前 ${stats.ratio.toFixed(2)}:1`;return ''}
  async function rebuildRows(){const token=Symbol('analysis');rebuildRows.token=token;$('resourceQueue').innerHTML='<div class="status-strip">正在分析图片…</div>';const next=[];for(let index=0;index<files.length;index++){const file=files[index];if(!IMAGE_RE.test(file.name)&&!['image/png','image/webp'].includes(file.type))continue;try{const stats=await analyzeImage(file);if(rebuildRows.token!==token)return;const kind=inferKind(file.name,stats),explicit=inferVariant(file.name);let targetId='',variant=explicit;if(kind==='logo'){targetId=longestPrefix(file.name,brandTargets())?.target?.id||bundle?.catalogImport?.brands?.[0]?.brandId||'';variant=variant||autoLogoVariant(stats)}else{targetId=longestPrefix(file.name,heroTargets())?.target?.id||bundle?.heroKey||'';variant=variant||'dark'}next.push({id:`r${index}`,file,index,stats,kind,targetId,variant,customKey:'',state:'pending',message:''})}catch(error){next.push({id:`r${index}`,file,index,stats:null,kind:'hero',targetId:'',variant:'dark',customKey:'',state:'invalid',message:`图片读取失败：${error.message||error}`})}}rows=next;recomputeRows();renderRows()}
  function effectiveTarget(row){return row.targetId==='__custom__'?stripHeroVariant(row.customKey):row.targetId}
  function recomputeRows(){const claims=new Set();for(const row of rows){if(!row.stats){row.state='invalid';continue}const target=effectiveTarget(row);if(!target){row.state='blocked';row.message='未匹配：请手动选择品牌 / Hero Key';continue}let error='';if(row.kind==='logo'){if(!brandById(target))error='请选择有效品牌';else if(!['light','dark','both'].includes(row.variant))error='请选择 Logo 版本';else error=validateLogo(row.stats,row.variant)}else{if(!KEY_RE.test(target))error='Hero Key 无效';else if(!['light','dark'].includes(row.variant))error='请选择 Hero Light / Dark';else error=validateHero(row.stats)}if(error){row.state='invalid';row.message=error;continue}const own=row.kind==='logo'?(row.variant==='both'?['light','dark']:[row.variant]).map(v=>`logo:${target}:${v}`):[`hero:${target}:${row.variant}`];if(own.some(c=>claims.has(c))){row.state='blocked';row.message='同一目标/版本已有更早文件；第一张优先，请手动改目标或版本';continue}own.forEach(c=>claims.add(c));if(row.kind==='hero'){const existing=heroExistingInfo(target).find(x=>x.variant===row.variant)?.current;row.state=existing?'update':'ready';row.message=existing?`已有 v${existing.version}；确认更新后发布下一版本`:'可新建发布'}else{row.state='ready';row.message='前端校验通过'}}updatePublishGate()}
  function targetOptions(row){if(row.kind==='logo')return `<option value="">请选择品牌</option>`+brandTargets().map(t=>`<option value="${escapeAttr(t.id)}" ${row.targetId===t.id?'selected':''}>${escapeHtml(t.brand.name||t.id)} · ${escapeHtml(t.id)}</option>`).join('');return `<option value="">请选择 Hero Key</option>`+heroTargets().map(t=>`<option value="${escapeAttr(t.id)}" ${row.targetId===t.id?'selected':''}>${escapeHtml(t.id)}</option>`).join('')+`<option value="__custom__" ${row.targetId==='__custom__'?'selected':''}>+ 手动输入新 Hero Key</option>`}
  function variantOptions(row){const list=row.kind==='logo'?[['light','Light'],['dark','Dark'],['both','Light + Dark']]:[['dark','Dark Hero'],['light','Light Hero']];return list.map(([v,label])=>`<option value="${v}" ${row.variant===v?'selected':''}>${label}</option>`).join('')}
  function filenamePreview(row){const target=effectiveTarget(row);if(!target)return '等待选择目标';if(row.kind==='logo'){const brand=brandById(target);if(!brand)return '无有效品牌';return row.variant==='both'?`${nextLogoFilename(brand,'light')} + ${nextLogoFilename(brand,'dark')}`:nextLogoFilename(brand,row.variant)}return nextHeroFilename(target,row.variant)}
  function renderRows(){const root=$('resourceQueue');root.innerHTML='';if(!rows.length){root.innerHTML='<div class="status-strip">尚未选择图片。</div>';updateQueueSummary();return}for(const row of rows){const el=document.createElement('div');el.className=`queue-row ${row.state}`;el.innerHTML=`<div class="file-meta"><div class="file-name">${escapeHtml(row.file.name)}</div><div class="file-facts">${row.stats?`${row.stats.width}×${row.stats.height} · ${row.stats.ratio.toFixed(2)}:1`:''}</div></div><select class="kind-select"><option value="logo" ${row.kind==='logo'?'selected':''}>品牌 Logo</option><option value="hero" ${row.kind==='hero'?'selected':''}>车型 Hero</option></select><div class="target-cell"><select class="target-select">${targetOptions(row)}</select>${row.kind==='hero'&&row.targetId==='__custom__'?`<input class="custom-key" type="text" value="${escapeAttr(row.customKey)}" placeholder="例如 byd-seal-08-2026" />`:''}</div><select class="variant-select">${variantOptions(row)}</select><div class="result-cell"><div class="result-name">服务器保存：${escapeHtml(filenamePreview(row))}</div><div class="result-status">${escapeHtml(row.message)}</div></div>`;root.appendChild(el);el.querySelector('.kind-select').onchange=e=>{row.kind=e.target.value;row.targetId='';row.variant=row.kind==='logo'?autoLogoVariant(row.stats):'dark';row.customKey='';recomputeRows();renderRows()};el.querySelector('.target-select').onchange=e=>{row.targetId=e.target.value;recomputeRows();renderRows()};el.querySelector('.variant-select').onchange=e=>{row.variant=e.target.value;recomputeRows();renderRows()};const custom=el.querySelector('.custom-key');if(custom)custom.oninput=e=>{row.customKey=normalizeToken(e.target.value);recomputeRows();renderRows()}}updateQueueSummary()}
  function updateQueueSummary(){const ready=rows.filter(r=>r.state==='ready').length,updates=rows.filter(r=>r.state==='update').length,bad=rows.filter(r=>['blocked','invalid'].includes(r.state)).length;$('queueSummary').textContent=`已分析 ${rows.length} 张 · 新增 ${ready} · 更新 ${updates} · 需处理 ${bad}`;$('recheckQueue').disabled=!files.length}

  function reviewState(){if($('bundleJson').value.trim()&&!bundle)return {blocked:true,reason:'资源包内容已修改，请先点“校验资源包”'};const bad=rows.filter(r=>['blocked','invalid'].includes(r.state));if(bad.length)return {blocked:true,reason:`还有 ${bad.length} 张图片需要手动匹配或修正`};if(bundleReview?.requiresConfig&&!$('configUpdateConfirm').checked)return {blocked:true,reason:'已有品牌/车型配置有变更，请确认差异'};const heroUpdates=rows.some(r=>r.kind==='hero'&&r.state==='update')||bundleReview?.requiresHero;if(heroUpdates&&!$('heroUpdateConfirm').checked)return {blocked:true,reason:'检测到重复/复用 Hero Key，请确认更新'};if(!rows.length&&!bundle)return {blocked:true,reason:'请提供资源包 JSON 或图片'};return {blocked:false,reason:'预检通过，可以发布'}}
  function updatePublishGate(){const r=reviewState();$('publishBundle').disabled=r.blocked;$('publishLog').textContent=r.reason;$('publishLog').className=`publish-log ${r.blocked?'warn':'ok'}`;updateQueueSummary()}
  async function adminJson(url,payload){const r=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json','X-Hero-Admin-Request':'1'},body:JSON.stringify(payload)}),data=await r.json();if(!r.ok)throw new Error(data.error||'请求失败');return data}
  async function adminFile(url,fields,file){const body=new FormData();Object.entries(fields).forEach(([k,v])=>body.append(k,v));body.append('file',file);const r=await fetch(url,{method:'POST',headers:{'X-Hero-Admin-Request':'1'},body}),data=await r.json();if(!r.ok)throw new Error(data.error||'上传失败');return data}
  function hasChanges(current,next,fields){return !current||fieldDiff(current,next,fields).length>0}
  async function applyConfig(info){const brandFields=['name','englishName'],vehicleFields=['brandId','series','modelName','modelYear','trimName','powertrainType','batteryCapacityKwh','rangeKm','rangeStandard','heroArtworkKey'];let current=(catalog.brands||[]).find(x=>x.brandId===info.brand.brandId);if(hasChanges(current,info.brand,brandFields))await adminJson('../api/brand/save',info.brand);await loadState({rebuild:false});current=(catalog.brands||[]).find(x=>x.brandId===info.brand.brandId);if(Boolean(current?.isActive)!==Boolean(info.brand.isActive))await adminJson('../api/brand/status',{brandId:info.brand.brandId,isActive:Boolean(info.brand.isActive)});await loadState({rebuild:false});let vehicle=(catalog.vehicles||[]).find(x=>x.catalogId===info.vehicle.catalogId);if(hasChanges(vehicle,info.vehicle,vehicleFields))await adminJson('../api/catalog/save',info.vehicle);await loadState({rebuild:false});vehicle=(catalog.vehicles||[]).find(x=>x.catalogId===info.vehicle.catalogId);if(Boolean(vehicle?.isActive)!==Boolean(info.vehicle.isActive))await adminJson('../api/catalog/status',{catalogId:info.vehicle.catalogId,isActive:Boolean(info.vehicle.isActive)});await loadState({rebuild:false})}
  async function publishAll(){const gate=reviewState();if(gate.blocked){updatePublishGate();return}$('publishBundle').disabled=true;try{let info=null;if(bundle)info=inspectBundle(bundle);if(info){$('publishLog').textContent='正在写入品牌/车型配置…';await applyConfig(info)}const active=rows.filter(r=>r.state==='ready'||r.state==='update');const total=active.reduce((sum,r)=>sum+(r.kind==='logo'&&r.variant==='both'?2:1),0);let n=0;for(const row of active){const target=effectiveTarget(row);if(row.kind==='logo'){for(const variant of row.variant==='both'?['light','dark']:[row.variant]){n++;$('publishLog').textContent=`上传 ${n}/${total} · Logo ${target} ${variant}`;const data=await adminFile('../api/brand/logo',{brandId:target,variant},row.file);row.message=`已发布 ${data.filename}`}}else{n++;const key=heroVariantKey(target,row.variant);$('publishLog').textContent=`上传 ${n}/${total} · Hero ${key}`;const data=await adminFile('../api/publish',{artworkKey:key},row.file);row.message=`已发布 ${data.filename}`}row.state='done';renderRows()}await loadState({rebuild:false});$('publishLog').textContent='整套资源发布完成。Catalog / Logo / Hero 已刷新到最新版本。';$('publishLog').className='publish-log ok'}catch(error){$('publishLog').textContent=`发布中止：${error.message||error}`;$('publishLog').className='publish-log error'}finally{updatePublishGate()}}

  function escapeHtml(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
  function escapeAttr(v){return escapeHtml(v)}function show(v){return v==null?'∅':typeof v==='object'?JSON.stringify(v):String(v)}
  function bind(){
    $('validateBundle').onclick=validateBundle;$('readClipboard').onclick=async()=>{try{$('bundleJson').value=await navigator.clipboard.readText();validateBundle()}catch(error){$('bundleSummary').textContent=`读取剪贴板失败：${error.message||error}`}};$('bundleJson').addEventListener('input',()=>{bundle=null;bundleReview=null;$('bundleSummary').textContent='内容已修改，请重新校验。';updatePublishGate()});$('configUpdateConfirm').onchange=updatePublishGate;$('heroUpdateConfirm').onchange=updatePublishGate;
    const input=$('resourceFiles'),drop=$('resourceDrop');input.onchange=()=>{files=Array.from(input.files||[]);void rebuildRows()};['dragenter','dragover'].forEach(t=>drop.addEventListener(t,e=>{e.preventDefault();drop.classList.add('dragging')}));['dragleave','drop'].forEach(t=>drop.addEventListener(t,e=>{e.preventDefault();drop.classList.remove('dragging')}));drop.addEventListener('drop',e=>{files=Array.from(e.dataTransfer?.files||[]);void rebuildRows()});$('recheckQueue').onclick=()=>void rebuildRows();$('refreshState').onclick=()=>void loadState();$('publishBundle').onclick=()=>void publishAll()
  }
  bind();updatePublishGate();void loadState();
})();
